import{where}from'firebase/firestore';import{many,one,documentId}from'./firestoreRepository';import{accessSchema,assignmentSchema,playerSchema,roleSchema,seasonSchema,teamSchema,userSchema}from'../validation/schemas';export const repositories={user:(id:string)=>one('users',id,userSchema),roles:(ids:string[])=>ids.length?many('roles',roleSchema,[where(documentId(),'in',ids.slice(0,30))]):Promise.resolve([]),access:(uid:string)=>many('userTeamAccess',accessSchema,[where('userId','==',uid),where('status','==','ACTIVE')]),teams:(ids:string[])=>ids.length?many('teams',teamSchema,[where(documentId(),'in',ids.slice(0,30)),where('status','==','ACTIVE')]):Promise.resolve([]),activeSeason:()=>many('seasons',seasonSchema,[where('isActive','==',true)]),async playerCount(teamId:string,seasonId:string){const a=await many('playerTeamAssignments',assignmentSchema,[where('teamId','==',teamId),where('seasonId','==',seasonId),where('status','==','ACTIVE')]);const ids=[...new Set(a.map(x=>x.playerId))];if(!ids.length)return 0;const groups=Array.from({length:Math.ceil(ids.length/30)},(_,i)=>ids.slice(i*30,i*30+30));return(await Promise.all(groups.map(g=>many('players',playerSchema,[where(documentId(),'in',g),where('status','==','ACTIVE')])))).flat().length}}
