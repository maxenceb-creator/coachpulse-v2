import{createContext,useContext,useEffect,useState,type ReactNode}from'react';import{onAuthStateChanged,signInWithEmailAndPassword,signOut,type User}from'firebase/auth';import{auth}from'../config/firebase';const C=createContext<{user:User|null;loading:boolean;login:(e:string,p:string)=>Promise<void>;logout:()=>Promise<void>}|null>(null);export function AuthProvider({children}:{children:ReactNode}){const[user,setUser]=useState<User|null>(null),[loading,setLoading]=useState(true);useEffect(()=>onAuthStateChanged(auth,u=>{setUser(u);setLoading(false)}),[]);return <C.Provider value={{user,loading,login:async(e,p)=>{await signInWithEmailAndPassword(auth,e,p)},logout:()=>signOut(auth)}}>{children}</C.Provider>}export const useAuth=()=>{const x=useContext(C);if(!x)throw Error('AuthProvider manquant');return x}
