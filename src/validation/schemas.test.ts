import{describe,expect,it}from'vitest';import{playerSchema,teamSchema}from'./schemas';describe('validation Firestore',()=>{it('refuse teamId dans Player',()=>expect(playerSchema.safeParse({playerId:'p',firstName:'A',lastName:'B',birthDate:new Date(),playerProfile:'FORWARD',preferredFoot:'RIGHT',status:'ACTIVE',teamId:'t'}).success).toBe(false));it('refuse le contexte FIRST_TEAM',()=>expect(teamSchema.safeParse({teamId:'t',name:'Première',teamType:'FIRST_TEAM',seasonId:'s',status:'ACTIVE'}).success).toBe(false))})
