rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DENY BY DEFAULT.
    // Les règles détaillées seront ajoutées par domaine avec tests Emulator.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
